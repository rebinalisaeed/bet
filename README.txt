Kurdliga separated files

HTML files now load CSS from /css and JavaScript from /js.
Keep your existing folders: assets/images/ and fonts/ in the same root.
No design changes were intentionally made; inline CSS/JS was only moved into separate files.

